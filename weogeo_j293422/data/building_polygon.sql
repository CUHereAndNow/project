CREATE TABLE building_polygon (
	`X` VARCHAR(32), 
	`Y` VARCHAR(32), 
	id INTEGER NOT NULL, 
	osm_id INTEGER NOT NULL, 
	z_order INTEGER NOT NULL, 
	building VARCHAR(12) NOT NULL, 
	`building:levels` VARCHAR(8), 
	height INTEGER, 
	name VARCHAR(69), 
	`name:en` VARCHAR(15), 
	craft VARCHAR(32), 
	office VARCHAR(23), 
	leisure VARCHAR(13), 
	shop VARCHAR(32), 
	`addr:housenumber` INTEGER, 
	`addr:housename` VARCHAR(32), 
	`addr:street` VARCHAR(21), 
	`addr:city` VARCHAR(7), 
	`addr:state` VARCHAR(4), 
	`addr:postcode` INTEGER, 
	`addr:place` VARCHAR(32), 
	`addr:country` VARCHAR(32), 
	`addr:hamlet` VARCHAR(32), 
	`addr:suburb` VARCHAR(32), 
	`addr:subdistrict` VARCHAR(32), 
	`addr:district` VARCHAR(32), 
	`addr:province` VARCHAR(32), 
	`addr:interpolation` VARCHAR(32), 
	website VARCHAR(66), 
	`contact:website` VARCHAR(32), 
	phone VARCHAR(32), 
	`contact:phone` VARCHAR(32), 
	opening_hours VARCHAR(32), 
	brand VARCHAR(32), 
	construction VARCHAR(11), 
	`tower:type` VARCHAR(32), 
	disused VARCHAR(32)
);
